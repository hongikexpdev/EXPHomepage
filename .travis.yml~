language: python
python:
    - "3.5.2"
install: "pip install -r requirements.txt"
script: nosetests

deploy:
  provider: heroku
  api_key:
    secure: 
  app: exp-homepage
  on:
    repo: hongikexpdev/exp-homepage
    branch: master
